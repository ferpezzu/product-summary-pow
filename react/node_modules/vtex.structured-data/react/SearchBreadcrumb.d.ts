import { FC } from 'react';
interface SearchBreadcrumbItem {
    name: string;
    href: string;
}
interface Props {
    breadcrumb?: SearchBreadcrumbItem[];
}
declare const SearchBreadcrumbStructuredData: FC<Props>;
export default SearchBreadcrumbStructuredData;
